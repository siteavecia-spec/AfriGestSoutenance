# Server environment example for AfriGest
# Copy this file to `.env` at the project root (same level as /server and /client)

# MongoDB connection string (Mongo Atlas or local)
# Example Atlas: mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/afrigest

# Database name override (if not provided in the URI path)
MONGODB_DBNAME=afrigest

# Server port
PORT=5000

# CORS allowed origin (your frontend URL)
CLIENT_URL=http://localhost:3000

# Node environment
NODE_ENV=development
